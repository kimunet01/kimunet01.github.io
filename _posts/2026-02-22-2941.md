---
title: "백준 1157"
date: 2026-02-22 01:15:00 +0900
categories: [Algorithm, 1일1백] # [대분류, 소분류] 순서입니다.
tags: [알고리즘, 백준]
---

❗ 백준 2941번 문제 : `크로아티아 알파벳`  
```
예전에는 운영체제에서 크로아티아 알파벳을 입력할 수가 없었다.
따라서, 다음과 같이 크로아티아 알파벳을 변경해서 입력했다.

č	➡       c=
ć	➡       c-
dž	➡       dz=
đ	➡       d-
lj	➡       lj
nj	➡       nj
š	➡       s=
ž	➡       z=

예를 들어, ljes=njak은 크로아티아 알파벳 6개(lj, e, š, nj, a, k)로 이루어져 있다.
단어가 주어졌을 때, 몇 개의 크로아티아 알파벳으로 이루어져 있는지 출력한다.

dž는 무조건 하나의 알파벳으로 쓰이고, d와 ž가 분리된 것으로 보지 않는다.
lj와 nj도 마찬가지이다. 위 목록에 없는 알파벳은 한 글자씩 센다.
```
💫 `접근 방법`
```
크로아티아 알파벳에 해당되는 알파벳이 있는 경우에 다음 인덱스를 확인.
다음 인덱스와 현재 인덱스의 알파벳 조합으로 만들어지는 경우에 단어수를 카운트.
인덱스도 함께 점프.
만약 뒤쪽에서 해당되는 알파벳이 존재할경우 Out of range가 발생 할 수 있으므로
조건문에서 남은 인덱스를 검사하여 방지.
```
⭕ `코드`
```c++
#include <iostream>
using namespace std;

int main() {
	string s;
	int count = 0;

	cin >> s;
	for (int i = 0; i < s.length(); ++i) {
		if (s[i] == 'c') {
			if (s.length() - i > 1 && s[i + 1] == '=') {
				count++;
				i++;
			}
			else if (s.length() - i > 1 && s[i + 1] == '-') {
				count++;
				i++;
			}
			else {
				count++;
			}
		}
		else if (s[i] == 'd') {
			if (s.length() - i > 2 && s[i + 1] == 'z' && s[i + 2] == '=') {
				count++;
				i += 2;
			}
			else if (s.length() - i > 1 && s[i + 1] == '-') {
				count++;
				i++;
			}
			else {
				count++;
			}
		}
		else if (s[i] == 'l') {
			if (s.length() - i > 1 && s[i + 1] == 'j') {
				count++;
				i++;
			}
			else {
				count++;
			}
		}
		else if (s[i] == 'n') {
			if (s.length() - i > 1 && s[i + 1] == 'j') {
				count++;
				i++;
			}
			else {
				count++;
			}
		}
		else if (s[i] == 's') {
			if (s.length() - i > 1 && s[i + 1] == '=') {
				count++;
				i++;
			}
			else {
				count++;
			}
		}
		else if (s[i] == 'z') {
			if (s.length() - i > 1 && s[i + 1] == '=') {
				count++;
				i++;
			}
			else {
				count++;
			}
		}
		else {
			count++;
		}
	}
	std::cout << count;
}
```