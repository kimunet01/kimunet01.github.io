---
title: "#2 엘레베이터 수정 및 상호작용 UI"
date: 2026-03-24 23:34:00 +0900
categories: [DevLog, The-Bear] # [대분류, 소분류] 순서입니다.
tags: [Unity]
---

`❗ 엘레베이터 상호작용시에 위치와 화면을 고정시키고, 상호작용시 UI를 화면에 출력 시킬 예정  `
<br>

**🔴 설계 목표**
```
엘레베이터 이벤트를 위해서 엘레베이터 내부 버튼 상호작용시에
지정된 위치로 이동 및 시선 고정을 할 예정.

기존의 상호작용 시에 디버그 로그가 뜨던 것을 UI로 바꿀 예정.
```

<br>

 **🟡 설계**
```
엘레베이터 내부 버튼 상호작용 -> 지정된 위치로 캐릭터 이동
-> 지정된 시선으로 고정 -> 엘레베이터 작동

InteractionUI 스크립트를 만들어서 Raycast에 닿았을 때 활성화 시켜주고,
Raycast에 아무것도 닿지 않을 때 다시 비활성화 하는 방식을 사용할 예정.
```

<br>

 **🔎 구현 전략 수립 - 엘레베이터**

`💭 어떤 방식으로 캐릭터를 고정 할 것인가?`
```
BedInteraction 할 때, 캐릭터의 움직임을 강제했던 방식처럼 하면 될 것 같음.
```

`📕 추가 변수`
```
1. 플레이어 고정 위치
2. 플레이어 시선 위치
```
<br>

`📗 수정 할 함수 목록`
```
1. MoveElevatorRoutine 일부
```
<br>

`🔴 구현 코드`

```csharp
    [Header("Player Control")] // 추가 변수
    [SerializeField] Transform playerStandPoint;
    [SerializeField] Vector3 playerStandRotation = new Vector3(0f,0f,0f);

    // ... (기존 코드)

    IEnumerator MoveElevatorRoutine(GameObject player)
    {
        isMoving = true;

        if (autoCloseCoroutine != null) StopCoroutine(autoCloseCoroutine);

        if (isDoorOpen) 
        {
            yield return StartCoroutine(CloseDoorRoutine());
        }

        CharacterController cc = player.GetComponent<CharacterController>();
        PlayerController pc = player.GetComponent<PlayerController>();

        // 물리 연산 끄기
        if (cc != null)
        {
            cc.enabled = false;
        }
        // 플레이어 제어 끄기
        if (pc != null) 
        {
            pc.isHandlingRoutine = true;
        }

        if (playerStandPoint != null)
        {
            float elapsed = 0f;
            float duration = 0.8f;

            // 시작 지점 저장
            Vector3 startPos = player.transform.position;
            Quaternion startRot = player.transform.rotation;
            float startCamX = pc.currentCameraRotationX;

            // 목표 지점 설정
            Vector3 targetPos = playerStandPoint.position;
            Quaternion targetRot = Quaternion.Euler(0, playerStandRotation.y, 0);
            float targetCamX = playerStandRotation.x;

            while (elapsed < duration)
            {
                elapsed += Time.deltaTime;
                float t = elapsed / duration;
                float smoothT = Mathf.SmoothStep(0f, 1f, t);

                // 위치 이동 및 시선 이동
                player.transform.position = Vector3.Lerp(startPos, targetPos, smoothT);
                player.transform.rotation = Quaternion.Slerp(startRot, targetRot, smoothT);
                if (pc != null && pc.playerCamera != null)
                {
                    pc.currentCameraRotationX = Mathf.Lerp(startCamX, targetCamX, smoothT);
                    pc.playerCamera.transform.localEulerAngles = new Vector3(pc.currentCameraRotationX, 0, 0);
                }

                yield return null;
            }
            player.transform.position = targetPos;
            player.transform.rotation = targetRot;
        }
        

        player.transform.SetParent(elevatorCar);

        Vector3 elevatorStartPos = elevatorCar.position;
        Vector3 elevatorTargetPos = isAtStartFloor ? targetFloorPoint.position : startFloorPoint.position;
        
        float distance = Vector3.Distance(elevatorStartPos, elevatorTargetPos);
        float elevatorDuration = distance / moveSpeed; 
        float time = 0;

        while (time < 1f)
        {
            time += Time.deltaTime / elevatorDuration;
            float smoothT = Mathf.SmoothStep(0f, 1f, time); 
            
            elevatorCar.position = Vector3.Lerp(elevatorStartPos, elevatorTargetPos, smoothT);
            yield return null;
        }
        
        elevatorCar.position = elevatorTargetPos; 

        player.transform.SetParent(null);

        // 물리 연산 및 제어 활성화
        if (cc != null)
        {
            cc.enabled = true;
        }
        if (pc != null) 
        {
            pc.isHandlingRoutine = false;
        }

        isAtStartFloor = !isAtStartFloor;
        yield return StartCoroutine(OpenDoorRoutine());
        isMoving = false;
    }
```
<br>

`💫 변수 사용을 위해서 일부 public 변수로 바꿈`
```
현재 ElevatorController 코드안에 코루틴이 들어가있지만 Sleep과 Sink는
플레이어 컨트롤러에 들어가있기 때문에 이후 수정이 필요 할 것 같음.
```

`⭕ 실행 결과`
![실행 결과](/assets/img/posts/2026-03-24-the-bear/Elevator_Move_Screen.gif)

<br>

 **🔎 구현 전략 수립 - 엘레베이터**

`💭 어떤 방식으로 UI를 출력 할 것인가?`
```
캔버스에 텍스트를 만들어 넣고, Raycast가 충돌시에 그 텍스트를 Active해주는 방식으로
하면 될 것 같음.
```

`📗 수정 할 함수 목록`
```
1. Interaction 함수
```
<br>

`🔴 구현 코드`

```csharp
    [Header("UI")]
    public UnityEngine.UI.Image blackFadeImage;
    public TMPro.TextMeshProUGUI dayTextUI;
    public TMPro.TextMeshProUGUI interactionTextUI; // 추가 변수
    
    // ... (기존 코드)

    if (Physics.Raycast(rayOrigin, rayDirection, out hitInfo, interactionDistance, interactionLayer))
        {
            if (hitInfo.collider.CompareTag("Door"))
            {
                hitDistance = hitInfo.distance;
                if (hitDistance <= safeInteractionDistance)
                {
                    Door doorScript = hitInfo.collider.GetComponentInParent<Door>();
                    Debug.DrawRay(rayOrigin, rayDirection * interactionDistance, Color.green);
                    if (doorScript.isOpen && doorScript.isMoving == false)
                    {
                        interactText = "E : 문 닫기";
                    }
                    else if (doorScript.isOpen == false && doorScript.isMoving == false)
                    {
                        interactText = "E : 문 열기";
                    }
                    canInteract = true;

                    // ... (이후 코드 동일)

                }
            }
        }

    // ... (이후 다른 오브젝트들에도 적용)

    if (interactionTextUI != null)
    {
        if (canInteract)
        {
            interactionTextUI.text = interactText; 
            if (!interactionTextUI.gameObject.activeSelf) 
            {
                interactionTextUI.gameObject.SetActive(true);
            }
        }
        else
        {
            if (interactionTextUI.gameObject.activeSelf) 
            {
                interactionTextUI.gameObject.SetActive(false);
            }
        }
    }
```
```
canInteract 변수를 만들어 상호작용 UI를 껐다가 켤 수 있도록 만듬.
```

`⭕ 실행 결과`

![실행 결과](/assets/img/posts/2026-03-24-the-bear/Interaction_UI_1.gif)
<br>

`🚩 문제 발생`

![수정1 작동화면](/assets/img/posts/2026-03-24-the-bear/Interaction_UI_2.gif)
```
싱크대 상호작용 & 침대 상호작용시에 화면이 암전이 되었을 때,
상호작용 문구가 계속 출력되는 문제 발생.
```

`💡 해결 방안`
```
암전이 될 때 플레이어의 움직임을 제한하였는데, UI를 띄우는 부분에서
플레이어의 움직임이 제한되었을때는 상호작용UI를 나타나게 하지않는
로직을 넣어주면 될 것 같음.
```

`🟢 수정 코드`
```csharp
    if (interactionTextUI != null)
    {
        // 추가 코드 : 플레이어 제어권이 없을 때, 상호작용 UI 비활성화
        if (isHandlingRoutine) 
        {
            if (interactionTextUI.gameObject.activeSelf) 
                interactionTextUI.gameObject.SetActive(false);
            return; 
        }

        if (canInteract)
        {
            interactionTextUI.text = interactText; 
            if (!interactionTextUI.gameObject.activeSelf) 
            {
                interactionTextUI.gameObject.SetActive(true);
            }
        }
        else
        {
            if (interactionTextUI.gameObject.activeSelf) 
            {
                interactionTextUI.gameObject.SetActive(false);
            }
        }
    }
```

`⭕ 최종 실행 결과`

![최종 실행 결과](/assets/img/posts/2026-03-24-the-bear/Interaction_UI_final.gif)

<br>

`📝 느낀점`
```
그래도 상호작용 UI를 넣으니 나름 그래도 게임같은 느낌이 나서 기분이가 좋다.
근데 띄엄띄엄 작업을 해서 스크립트 통일성, 변수 이름 통일성 이런게 하나도 없다..
나중에 시간 나면 한번에 확 통일해야 할 것 같다.
```